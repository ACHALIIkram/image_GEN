# FaceDiT — dépendances épinglées (NF-8)
#
# Ces versions sont celles de l'environnement où les résultats publiés ont été produits,
# relevées par `pip freeze` et non recopiées d'une intention. Python 3.12.6.
#
# PyTorch s'installe séparément, depuis son propre index :
#
#   GPU NVIDIA (CUDA 12.6)
#     pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu126
#   CPU seul — suffisant pour la démonstration (≈ 5 s par image au lieu de 0,8 s)
#     pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cpu
#
# puis :
#   pip install -r requirements.txt

# --- coeur ---
# numpy RESTE en 1.x : mediapipe 0.10.21 déclare `numpy<2`, étant compilé contre l'ABI
# NumPy 1. La version précédente de ce fichier épinglait numpy==2.1.3 tout en laissant
# mediapipe en commentaire — l'installation des deux ensemble est impossible. Comme le
# masque de peau de MediaPipe conditionne toute la mesure d'ITA, c'est numpy qui cède.
numpy==1.26.4
pyyaml==6.0.2
tqdm==4.67.1
pandas==2.2.3
pillow==11.1.0
scipy==1.15.2
opencv-python==4.10.0.84

# --- diffusion (planning de bruit + VAE gelé, jamais le modèle génératif) ---
diffusers==0.32.2
safetensors==0.5.2
huggingface-hub==0.36.2
# accelerate n'est pas optionnel en pratique : sans lui, diffusers charge le VAE en
# doublant son empreinte RAM, ce qui compte sur les 16 Go de la machine cible.
accelerate==1.3.0

# --- oracle / évaluation ---
clean-fid==0.1.35
lpips==0.1.4
scikit-learn==1.6.1

# --- mesure du teint ---
# Requis, et non optionnel comme le laissait entendre la version précédente de ce fichier.
# `data.ita_use_mediapipe` vaut true dans configs/base.yaml : sans MediaPipe, le masque de
# peau retombe sur une ellipse géométrique qui inclut cheveux et arrière-plan, et l'ITA
# mesuré n'est plus le même — les chiffres publiés ne seraient pas reproduits.
mediapipe==0.10.21

# --- interface & suivi ---
# 5.44.1 et non 5.16.0 : sur le couple gradio 5.16.0 / gradio_client 1.7.0, la
# génération du schéma d'API lève `TypeError: argument of type 'bool' is not iterable`
# (gradio_client/utils.py, get_type) dès que l'interface contient nos composants. Gradio
# masque l'erreur derrière un message trompeur — « When localhost is not accessible, a
# shareable link must be created » — qui envoie chercher un problème de réseau ou de
# proxy inexistant. Ne pas redescendre sous cette version.
gradio==5.44.1
tensorboard==2.18.0
matplotlib==3.10.1
seaborn==0.13.2

# --- jeu de données (entraînement et évaluation seulement, pas la démonstration) ---
datasets==3.2.0

# --- optionnel ---
# timm==1.0.14   # backbone DINOv2 pour la variante FD-DINOv2 du FID (F-E8). Absent de
#                # l'environnement de mesure : cette métrique n'a donc pas été calculée,
#                # et le rapport ne la présente pas.
